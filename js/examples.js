// практика задача
// const randomArr = (amount, min, max) => {
//   let num = [];
//   for (let i = 0; i < amount; i += 1) {
//     num.push(parseInt(Math.random() * (max - min) + min));
//   }

//   let sumItem = 0;
//   for (let item of num) {
//     sumItem += item;
//   }

//   return sumItem / num.length;
// };

// console.log(randomArr(10, 1, 100));

// задание 2, длинный вариант решения!!!!

// const calculateEngravingPrice = function (message, pricePerWord) {
//   //message.split(' ');
//   //message = message.split(' ').length;

//   return message.split(' ').length * pricePerWord;
//   // let messageSum = message.length;
//   // return messageSum * pricePerWord;
// };
